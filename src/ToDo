Done:
    Added ability to sort media items into folders
    Fixed black/logo screen buttons on web remote not working
    Fixed Title not saving in new custom slide
    Fixed custom slide data being retained after saving new slide
    Add copying/thumbnail feedback when importing a video
    Figure out why new videos aren't saving to database
    See about changing thumbnails for video import to bytes instead of files
    Style QProgressBar
    Unload video if loaded during remove
    Add video playing progress bar or slider
    Fix Ignore update not working
    Figure out why preview label in live widget isn't staying centered for videos and web slides
    Figure out why video isn't going back to zero at end after scrubbing
    Set window positions and maximized states before showing
    Consolidated all display widgets into the DisplayWidget class, switched DisplayWidget to a QStackedWidget

To Do:
    Add stretch/fit/fill to settings and modify lyric widget to use settings

Future Releases:
    Video backgrounds
    Allow sorting media into folders
