Done:
    Auto-scroll OOS list when adding an item
    Except no server check timer on shutdown (for quick shutdowns)
    Add wait splash after importing image (maybe video too?)
    Do all slide parsing when populating widget or finishing editing; remove calls to do so from everywhere else
    Fix folder expand arrow color on dark theme
    Allow sorting media into folders
    Added types to method/function arguments
    Make sure delete is only removing the tree item, not running populate

To Do:

Future Releases:
    Video backgrounds
    When running under Linux, removing an image results in a hard crash with no feedback
