Done:
    Reduce parse time in send to preview
    remove all references to parse all songs
    attach parse scripture to calculate
    Attach countdown widget to display widget
    Fixed custom slide with no words not being sent to preview
    Fixed media player not stopping when changing slides
    Fix scripture that spans across chapter!
    Fixed display screen not positioned correctly on dual-monitor setups
    Fixed crash on "None" for verse number in parse_scripture_by_verse
    Fix export crash

To Do:
    Fix lyric widget sizing issue when parsing songs on hidden display

Future Releases:
    Video backgrounds
